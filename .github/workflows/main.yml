on:
  push:
    branches:
      - main
      - Development

jobs:
  create_arch_images:
    runs-on: ubuntu-latest

    name: An automated way to create the file
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: list files
        run: ls

      - name: Create the img
        run: DOCKER_BUILDKIT=1 docker build --file Dockerfile --output out . 
      
      - name: list now the files
        uses: actions/upload-artifact@v3
        with:
          name: artefacto
          path: out/

      - name: Get the opt
        run: echo "the time was ${{ steps.create.outputs.files }}"

